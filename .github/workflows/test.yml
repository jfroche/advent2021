# Generated by ci/ci_tool.cue; do not edit

name: Tests
on:
  push:
    branches:
    - '**'
    tags-ignore:
    - v*
  pull_request: {}
jobs:
  fmt:
    runs-on: self-hosted
    defaults:
      run:
        shell: bash
    steps:
    - name: Install nix
      uses: cachix/install-nix-action@v16
    - name: Install cachix
      uses: cachix/cachix-action@v10
      with:
        name: advent-2021
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        extraPullNames: nix-community
        pushFilter: (-source$)
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Check fmt
      run: nix develop --ignore-environment -c treefmt --fail-on-change
    - name: Check clean checkout
      uses: numtide/clean-git-action@v1
  lint:
    runs-on: self-hosted
    defaults:
      run:
        shell: bash
    steps:
    - name: Install nix
      uses: cachix/install-nix-action@v16
    - name: Install cachix
      uses: cachix/cachix-action@v10
      with:
        name: advent-2021
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        extraPullNames: nix-community
        pushFilter: (-source$)
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Lint
      run: nix develop --ignore-environment -c make lint
    - name: Check clean checkout
      uses: numtide/clean-git-action@v1
  test:
    runs-on: self-hosted
    defaults:
      run:
        shell: bash
    steps:
    - name: Install nix
      uses: cachix/install-nix-action@v16
    - name: Install cachix
      uses: cachix/cachix-action@v10
      with:
        name: advent-2021
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        extraPullNames: nix-community
        pushFilter: (-source$)
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Test
      run: make build
    - name: Check clean checkout
      uses: numtide/clean-git-action@v1
  test-cached-cargo:
    runs-on: self-hosted
    defaults:
      run:
        shell: bash
    steps:
    - name: Install nix
      uses: cachix/install-nix-action@v16
    - name: Install cachix
      uses: cachix/cachix-action@v10
      with:
        name: advent-2021
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        extraPullNames: nix-community
        pushFilter: (-source$)
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Cache cargo registry
      uses: actions/cache@v2
      with:
        path: ~/.cargo
        key: cargo-registry-${{ hashFiles('**/Cargo.lock') }}
    - name: Cache cargo target
      uses: actions/cache@v2
      with:
        path: target
        key: cargo-target-${{ hashFiles('**/Cargo.lock') }}
    - name: Test
      run: nix develop --ignore-environment -c make test
    - name: Check clean checkout
      uses: numtide/clean-git-action@v1
